- **refactor(providers):** `moonshot`, `vertex-partner` and `qianfan` were second registrations of services already served by `kimi`, `vertex` and `baidu`: same endpoint, same credentials, same account. Each split tracked its own quota and cooldown state against a single real upstream quota and listed the same models twice. They are merged into their canonical ids, which take the union of both catalogs (`vertex` gains `claude-opus-4-8` and `claude-opus-4-6`; `baidu` gains `ernie-5.0-thinking-latest`, `modelsUrl` and the 128k default). Retired ids still resolve via `LEGACY_PROVIDER_ID_MAP`, so stored connections and `moonshot/<model>`-style references keep working. Provider count 248 to 245. Migration `122` folds the three ids across every provider-keyed table (#26).
- **refactor(providers):** `glm` and `glmt` now share one connection pool instead of tracking separate quota against the same z.ai coding-plan key. They stay distinct providers because `glmt` is not a cosmetic duplicate: it carries a real thinking preset (65536 max tokens, 24576 thinking budget, adaptive) that `glm` lacks, and `GlmExecutor` resolves its config by provider id, so retiring the id would have silently fallen back to the OpenAI config and routed z.ai traffic to `api.openai.com`. The existing `nvidia`/`nvidia_nim` special case is generalized into `SHARED_CREDENTIAL_POOLS` with its behaviour unchanged (#26).
- **fix(providers):** several pre-existing asymmetries between the duplicate pairs, each of which the merge would otherwise have made worse: `baidu` was sending a `system` role to an ERNIE endpoint that rejects it (only the duplicate `qianfan` was flagged in `memory/injection`), `kimi` had no live model discovery (it was registered under `moonshot`), Chinese-model combo routing knew `qianfan` but not `baidu`, `glmt` was metered despite sharing `glm`'s flat-rate coding plan, and the MITM host map attributed `api.moonshot.ai` to the retiring id (#26).
