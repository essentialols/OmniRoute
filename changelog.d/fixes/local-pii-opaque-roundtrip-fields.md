- **fix(sse):** PII sanitization no longer rewrites opaque round-trip blobs (`encrypted_content`, `signature`, `thought_signature`). Base64url ciphertext can contain a chance `-pk_<20+ alnum>_` run that the generic api-key pattern matched, and splicing `[API_KEY_REDACTED]` into a Codex reasoning blob made every later turn of that conversation fail upstream with `400 invalid_encrypted_content`.
