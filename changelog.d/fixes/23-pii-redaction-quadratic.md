- **fix(pii):** `redactPIIForCapture` and `sanitizePII` rebuilt the whole string once per replacement range, making redaction O(matches x length). A 1.72MB embeddings response spent ~1.9s in that loop versus ~79ms for the regex scans themselves, blocking the event loop and starving every concurrent request. Ranges are now emitted left-to-right in a single pass (1.72MB capture redaction 5002ms to 95ms; 512KB `sanitizePII` 549ms to 29ms; slowest of 10 concurrent embedding batches 81.3s to 4.5s) (#23).
